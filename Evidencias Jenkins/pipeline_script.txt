node {
    stage ('Preparation') {
        catchError (buildResult: 'SUCCESS') {
            sh 'docker stop indicator-running'
            sh 'docker rm indicator-running'
        }
    }
    stage ('Build') {
        build 'BuildAppJob'
    }
}
